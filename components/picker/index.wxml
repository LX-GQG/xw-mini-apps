<picker class="picker" bindchange="onChange" value="{{ index }}" range="{{ optionList }}">
  <input-test writable="{{ false }}" required label="{{ title }}" value="{{ value }}" />
  <image class="icon-down" src="../../images/down.png"></image>
</picker>